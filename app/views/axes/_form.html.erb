<script>
    $(document).ready(function() {
       $(".select").select2({
          language: "he",
          theme: "bootstrap"
      }); });
    $.fn.modal.Constructor.prototype.enforceFocus = function() {};
</script>

<%= simple_form_for(@axis, html: { class: 'form-inline' }) do |form| %>
  <% if axis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(axis.errors.count, "error") %> prohibited this axis from being saved:</h2>

      <ul>
      <% axis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.input :name %>
  </div>
  <div class="field">
    <%= form.input_field :head_ids, as: :select, multiple: "multiple",  input_html: {class: "select"}, collection: Head.all, label_method: :name, value_method: :id %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
