<script>
    $(document).ready(function() {
       $(".select").select2({
          language: "he",
          theme: "bootstrap"
      }); });
    $.fn.modal.Constructor.prototype.enforceFocus = function() {};
</script>


<%= form_tag(buses_path, :method => "post", id: "pop-form", class: "form-inline my-2 my-lg-0") do %>
  <%= text_field(:bus, :name) %>
  <%= select_tag "city", options_for_select(Kid.pluck(:city).uniq), multiple: true, class: "select" %>
  <%= submit_tag "הוספה", class: "btn btn-outline-success my-2 my-sm-0" %>
<% end %>
